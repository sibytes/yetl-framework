apiVersion: yetl-framework.io/en/1/datastore
adls:
  default:
    id: default
    name: jaffleshop
    description: Landing ADLS store for {{datastore_name}}
    container: landing
    schema: default
    account: adlsgeneva
    abfss: "{{container}}@{{account}}.dfs.core.windows.net"
    dbfs: "mnt/{{container}}"
  datastores:
    - id: landing_jaffleshop_jafflshop
      schema: jaffleshop
    - id: landing_jafflshop_stripe
      schema: stripe


