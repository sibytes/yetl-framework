apiVersion: "yetl-framework.io/en/1/dataset"
deltalake:
  default:
    name: "default"
    table_name: "{{datastore_schema}}_{{dataset}}"
    path: "{{datastore_container}}/{{datastore_name}}/{{dataset_classification}}/{{datastore_schema}}"
    write_options: 
      partition:
        - "partition"
      mode: "append"
      mergeschema: true
  datasets:
