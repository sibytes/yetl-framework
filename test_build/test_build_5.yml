dataset:
    default:
        apiVersion:
            base: Dataset
            namespace: yetl-framework.io
            type: Deltalake
            version: '1'
        name: _
        path: ///
        write_options:
            mergeschema: true
            mode: append
            partitions:
            - partition
